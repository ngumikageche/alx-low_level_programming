#include<string.h>
/**
 *_strcpy-a function that copies the string
 *@src:pointer by
 *@dest:pointer to destination
 *Return: value :the pointer dest
 */
char *_strcpy(char *dest, char *src)
{
return (strcpy(dest, src));
}
