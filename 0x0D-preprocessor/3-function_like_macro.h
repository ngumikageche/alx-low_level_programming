#ifndef HEADER_FILE
#define HEADER_FILE
/**
 *File: 3-function_like_macro.h
 *@x: number
 *Desc: Header file that defines a function macro ABS(x)
 *     that computes the absolute value of a number x.
 */
#define ABS(x) ((x) < 0 ? (-x) : (x))
#endif
